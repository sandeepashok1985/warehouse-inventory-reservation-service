# Testcontainers configuration for Docker Desktop on Windows
# Docker Desktop 29.4.1 uses the dockerDesktopLinuxEngine pipe
docker.host=npipe:////./pipe/docker_cli
# Pin API version to one compatible with docker-java 3.3.x
docker.api.version=1.44
